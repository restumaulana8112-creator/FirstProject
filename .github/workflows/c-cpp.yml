name: C++ MSVC Build

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Compile Code
      run: cl /EHsc nomor10.cpp

    - name: Run Tester
      run: .\nomor10.exe